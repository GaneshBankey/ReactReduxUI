export function authHeader() {
    // return authorization header with jwt token
    //let user = JSON.parse(localStorage.getItem('user'));

    //if (user && user.token) {
        return { 'Authorization': 'AuthToken=73ea9001-05f2-4eff-b61b-1f676fcd302f'};
    //} else {
     //   return {};
    //}
}